# Timing outputs

Each remaining figure package writes one TSV here with:
`package_id`, `operation`, `elapsed_seconds`, and `details`.

At minimum, separately time data loading, substantial in-memory derivation or
simulation, figure rendering, and the complete run.
