# Posterior strategy context supplement

The requested composite uses a two-column by three-row layout:

- left column: posterior support, `Pr(H/L > 1)`;
- right column: posterior median `log(H/L)`;
- rows: four-line, all-line, and unified single-line fitting contexts.

Each subpanel contains day 4 and day 6 heatmaps at 1× fitted seeding density.
All five Pareto models and all cell lines available in each context are shown.
The 0AA schedule is omitted, following the standing figure-revision decision.
No semantic interpretation or legend drafting is included.

Visual inspection, visual QC, and postflight polishing validation are explicitly
deferred for user-assisted review.
