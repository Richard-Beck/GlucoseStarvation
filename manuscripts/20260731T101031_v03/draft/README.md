# Draft

`manuscript_draft.html` is a self-contained review copy. All figure PNGs and
styles are embedded, so the file can be opened or shared without its source
directory. Regenerate it using `rebuild/manuscript/build_manuscript_html.py`.
Invoke Python rebuild scripts through `rebuild/python_runner.sh` so the package
does not depend on the HPC system Python version.
