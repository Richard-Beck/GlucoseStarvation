# Traceability

`upstream_input_register.tsv` records the five finalized component roots and
deterministic directory hashes. `copied_input_identity.tsv` authenticates each
copied manuscript-facing source and figure. `figures/` preserves the complete
integration, byte-identity, visual-QC, and omission records.
